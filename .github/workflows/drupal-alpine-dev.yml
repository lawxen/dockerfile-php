name: drupal-alpine-dev

on: workflow_dispatch

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version:
          - folder: 'php7.4/alpine-dev'
            tag: 'php7.4-alpine-dev'
          - folder: 'php7.4/alpine-d9-dev'
            tag: 'php7.4-alpine-d9-dev'
          - folder: 'php8.2/alpine-dev'
            tag: 'php8.2-alpine-dev'
          - folder: 'php8.2/alpine-d10-dev'
            tag: 'php8.2-alpine-d10-dev'
          - folder: 'php8.3/alpine-dev'
            tag: 'php8.3-alpine-dev'
          - folder: 'php8.3/alpine-d10-dev'
            tag: 'php8.3-alpine-d10-dev'

    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          context: .
          platforms: linux/amd64,linux/arm64
          file: 'drupal/alpine/${{ matrix.version.folder }}/Dockerfile'
          tags: 'lawxen/drupal:${{ matrix.version.tag }}'

name: drupal-debian-dev

on: workflow_dispatch

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version:
          - folder: 'php7.4/debian-dev'
            tag: 'php7.4-debian-dev'
          - folder: 'php7.4/debian-d9-dev'
            tag: 'php7.4-debian-d9-dev'
          - folder: 'php8.2/debian-dev'
            tag: 'php8.2-debian-dev'
          - folder: 'php8.2/debian-d10-dev'
            tag: 'php8.2-debian-d10-dev'
          - folder: 'php8.3/debian-dev'
            tag: 'php8.3-debian-dev'
          - folder: 'php8.3/debian-d10-dev'
            tag: 'php8.3-debian-d10-dev'

    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      -
        name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      -
        name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          context: .
          platforms: linux/amd64,linux/arm64
          file: 'drupal/debian/${{ matrix.version.folder }}/Dockerfile'
          tags: 'lawxen/drupal:${{ matrix.version.tag }}'
